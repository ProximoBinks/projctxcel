/**
 * The single public, canonical origin for Simple Tuition.
 *
 * Keep canonical and structured-data URLs independent of deployment-preview
 * environment variables so previews cannot accidentally become index targets.
 */
export const SITE_URL = "https://simpletuition.com.au";

